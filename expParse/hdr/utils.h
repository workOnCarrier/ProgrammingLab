#ifndef __UTILS_EXP_H__ 
#define __UTILS_EXP_H__

#include <list>

enum ERROR_STATUS {
SUCCESS,
FAILURE
};

enum TOKEN_TYPE {
NUMBER,
OPERATOR,
BRACKET_START,
BRACKET_END
};

#endif  //__UTILS_EXP_H__
